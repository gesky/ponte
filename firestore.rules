rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Qualquer usuario autenticado pode ler e escrever
    // Suficiente para MVP — restringir apos crescimento
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
